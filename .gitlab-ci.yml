stages:
    - package
    - sast
    - sca-package
    - publish
    - sca-container
    - deploy
    - fuzzing
    - dast

package:
  stage: package
  image: node:20
  script:
    - npm install
  artifacts:
    paths:
      - node_modules/
    expire_in: 1 hour